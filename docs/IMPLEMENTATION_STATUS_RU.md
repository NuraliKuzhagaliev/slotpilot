# Статус реализации · этап 2

## Последняя проверка · 24 сентября 2026, повторное исправление голоса

Текущая локальная версия: **196/196 тестов**, TypeScript, Next.js и Vinext production builds, ESLint изменённых файлов — успешно. Воспроизведены и устранены зависание при `reply.done` перед `tool.call`, молчание при ошибке JSON аргументов и обрыв новой речи старым событием. Добавлены буфер PCM с ограниченным ожиданием, защита повторного запуска, компактный результат инструмента и контекст английского распознавания. Настоящий AudioWorklet проверен в Edge с синтетическим потоком на 44,1/48 кГц. Это не live microphone/AssemblyAI-проверка. Публикация новой версии на пользовательский Vercel и живой разговор ещё необходимы; полная P0-приёмка не заявляется. См. `docs/VOICE_VALIDATION_RU.md`.

Ниже сохранена история прежних этапов и их проверок.

## Написано и проверено доступными средствами

- Next.js UI, адаптивный booking экран, Live Board, карточки и объяснения.
- Прежний Constraint Engine, до трёх услуг, единый резерв, бюджет/даты/готовность/филиалы, совместимые мастер и пост.
- Серверные запросы и версии; подготовка, evidence, подтверждение, сверка по actionId.
- PostgreSQL-миграция, CAS-транзакция и exclusion constraints. **Миграция применена к бесплатному Supabase; PostgreSQL-конкуренция и откат проверены.**
- Перенос/отмена, тестовая доступность расходников, сохранение обращения администратору.
- Admin и защищённые reset/busy/occupy/part операции.
- Временные голосовые токены, лимиты, английский prompt, десять инструментов, быстрые перебивания и очистка звука.
- Раздельные client/admin пароли, подписанные cookie, same-origin проверки, проверка владельца.
- Честный preview подбора при отсутствии Supabase.

## Фактические проверки

175/175 Node-тестов: 143 прежних, 22 новых операций, 6 mock-voice, 4 контрактных. Строгий TypeScript. Next.js production build и Sites/Vinext build. Реальный браузерный UI-проход центрального подбора до предложения North 14:00–16:00, 120 мин, 37 000 KZT.

## Что не считать готовым

P0 ещё **не принят целиком**: не выполнен новый полный голосовой путь до сохранения, нет десяти живых прогонов и резервного видео. Результат старого probe подтверждён пользователем, но новая интеграция изменена.

Требуют дальнейшего завершения: перенос тестов в Vitest/Playwright, полная RU-локализация (EN готов), более полная поддержка голосового reschedule без ручного открытия нового запроса. Без настроенных секретов опубликованное приложение работает только как scheduling preview.

P2/Gateway не реализуется как необязательный. Реальная телефония, платежи, CRM и реальный склад исключены по ТЗ.

Опубликован приватный preview: https://slotpilot-nurali.nurali6002217.chatgpt.site . Успешный деплой подтверждён хостингом. Серверные секреты базы настроены; для голоса остаётся ASSEMBLYAI_API_KEY.

12 интеграционных сценариев реальных route handlers и Supabase успешно пройдены. Это вызовы HTTP Request/Response в процессе Node, не имитация базы и не браузерный end-to-end. Полный лог — `docs/stage3/route-integration.log`.

## Голосовая стабильность · 24 сентября 2026

- PCM-воспроизведение сглаживает краткие провалы между пакетами через границы аудиоблоков; очистка ответа затухает от последнего действительно воспроизведённого отсчёта, чтобы не создавать скачок громкости.
- Браузерное шумоподавление отключено при сохранённом эхоподавлении в соответствии с актуальной документацией AssemblyAI. Голосовые HTTP-запросы имеют таймаут 15 секунд вместо неограниченного ожидания; после таймаута записи результат остаётся неопределённым до серверной сверки.
- Локальные mock/PCM-проверки дополнены тестами короткого провала, очистки очереди и таймаута. Они не измеряют задержку реального ответа или звук на устройстве. Обновлённый живой разговор с серверным ключом AssemblyAI и микрофоном ещё необходим; утверждать стабильные 1–2 секунды нельзя.
- Проверки этой правки: `npm test` — 189/189; `npm run typecheck`, `npm run build:next` и `npm run build` (Vinext) — успешно; ESLint для изменённых JS-файлов — без замечаний. Общий `npm run lint` пока выдаёт 17 ошибок и 7 предупреждений в других, ранее существовавших файлах.

## Исправление блокировки голосового запуска · 24 сентября 2026

В опубликованной версии сообщение `Too many attempts` пришло из PostgreSQL rate limiter: для одного пользователя в namespace `slotpilot-public` было 7 попыток при ограничении 3 запуска за 10 минут. В той же серверной сессии уже было 10 записей о выдаче токенов за скользящие 24 часа при лимите 10. Раньше счётчик суточной квоты увеличивался ещё до ответа AssemblyAI, поэтому неудачная выдача токена тоже расходовала квоту.

Новая версия допускает до 10 попыток подключения за 10 минут, но сохраняет суточный лимит выданных токенов и общий предел 30. Неуспешная выдача токена либо отказ ограничителя освобождают зарезервированную суточную попытку. При суточном лимите интерфейс получает время, когда освободится первое место, в UTC. Уже сохранённые в базе старые записи о токенах автоматически не удаляются: достоверно отличить успешную выдачу от прежней неудачной попытки по ним нельзя.

Проверки: 190/190 тестов, TypeScript, Next/Vinext production build и ESLint для изменённых файлов проходят. Новый тест route handler проверяет отказ провайдера, возврат квоты, короткий лимит и успешную выдачу; реальный разговор этим тестом не подтверждается.
