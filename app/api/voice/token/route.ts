import { requireUser,sameOrigin } from '@/src/server/app/auth';
import { handle,json,body } from '@/src/server/app/http';
import { AppError,mutate,rateLimit } from '@/src/server/app/db';
import { request,session } from '@/src/server/app/state';
import { bookingSession } from '@/agents/booking';
import { z } from 'zod';
export async function POST(req:Request){return handle(async()=>{sameOrigin(req);const u=await requireUser(req);z.object({consent:z.literal(true)}).strict().parse(await body(req));const key=process.env.ASSEMBLYAI_API_KEY;if(!key)throw new AppError('KEY_NOT_CONFIGURED','Voice is not configured. You can still book using the form.',503);await rateLimit(`voice-${u.userId}`,3,600);await rateLimit('voice-global',30,86400);const now=new Date().toISOString();const result=await mutate((s)=>{const row=session(s,u);row.tokenTimes=row.tokenTimes.filter(t=>t>Date.now()-86400000);const limit=Math.min(30,Math.max(1,Number(process.env.VOICE_DAILY_TOKEN_LIMIT)||10));if(row.tokenTimes.length>=limit)throw new AppError('VOICE_LIMIT','Today’s demo voice limit is reached. Use the form instead.',429);row.tokenTimes.push(Date.now());return request(s,u)});const duration=Math.min(600,Math.max(60,Number(process.env.VOICE_MAX_SESSION_SECONDS)||300));const url=new URL('https://agents.assemblyai.com/v1/token');url.searchParams.set('expires_in_seconds','60');url.searchParams.set('max_session_duration_seconds',String(duration));let response;try{response=await fetch(url,{headers:{Authorization:`Bearer ${key}`},redirect:'error',signal:AbortSignal.timeout(12000)})}catch{throw new AppError('PROVIDER_UNREACHABLE','Could not reach AssemblyAI.',502)}if(!response.ok)throw new AppError('PROVIDER_ACCESS_DENIED','Check Voice Agent API access and account credits.',502);const data=await response.json() as {token?:unknown};if(typeof data.token!=='string'||data.token.length>16000)throw new AppError('PROVIDER_ERROR','Invalid provider token response.',502);return json({token:data.token,websocketUrl:'wss://agents.assemblyai.com/v1/ws',maxSessionSeconds:duration,session:bookingSession(now,result.value)})})}
